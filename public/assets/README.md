# Assets Folder

This folder contains static assets for the ResumeForge application, including:

- Template preview images
- Icons and graphics
- Other static media files

## Template Images

Template preview images should be stored here and referenced in the `placeholder-images.json` file using relative paths from the `src/` directory.

Example path format: `/assets/template-name.jpg`
